import path from 'path';

import { flattenObject } from './index';

const folderName = __dirname.split('\\').pop()!;

describe(folderName, () => {
	it.todo('will flatten an object');
	it.todo('will not modify the initial object');
	it.todo('will do <something> if the input is not an object');
	it.todo('will use a custom separator if passed');
});
